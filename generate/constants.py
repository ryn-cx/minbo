# TODO: Validate
"""Constants."""

from pathlib import Path

FILES_PATH = Path(__file__).parent / "_files"
"""Where the recorded responses live."""

MINBO_PATH = Path(__file__).parent.parent / "src" / "minbo"
"""The package the models are written into."""
